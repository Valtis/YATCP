program: |

    extern fn int_printer(value: int);

    fn main() : int {
        let a = A {
          a = 14;
        };

        int_printer(a.b);
    }
    
    struct A {
        a: int;
    }
    
expect_compile_failure: true
expect_stderr: |
  9:19 Name error: Undefined field 'b' for struct 'A'
      int_printer(a.b);
                    ^
  Terminating compilation due to 1 error