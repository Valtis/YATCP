program: |

    fn main() : int {
      val a = A {
          a = 4;
      };
      a.a = 5;
      return 0;

    }
    
    struct A {
        a: int;
    }
    
expect_compile_failure: true
expect_stderr: |
  6:7 Type error: Cannot assign to read only variable
    a.a = 5;
        ^
  3:7 Note: Variable 'a', declared here, has been declared as read only
    val a = A {
        ^
  Terminating compilation due to 1 error