program: |

    fn main() : int {
       let x = A {
              a = 4 + "hello";
       };
    }
    
    struct A {
       a: int;
    }

expect_compile_failure: true
expect_stderr: |
  4:17 Type error: Incompatible operand types 'Integer' and 'String' for this operation
            a = 4 + "hello";
                  ^
  Terminating compilation due to 1 error