program: |
    fn main() : int {
      let a: int[0] = 4;
    }

expect_compile_failure: true
expect_stderr: |
  2:7 Type error: Array index negative or zero (0)
    let a: int[0] = 4;
        ^
  Terminating compilation due to 1 error