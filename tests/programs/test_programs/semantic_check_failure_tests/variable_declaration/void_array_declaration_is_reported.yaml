program: |
    fn main() : int {
      let a: void[40] = 4;
    }

expect_compile_failure: true
expect_stderr: |
  2:7 Syntax error: Void is not valid base type for an array
    let a: void[40] = 4;
        ^
  Terminating compilation due to 1 error