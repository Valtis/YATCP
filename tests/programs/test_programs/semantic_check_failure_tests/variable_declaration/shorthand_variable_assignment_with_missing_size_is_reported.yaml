program: |
    fn main() : int {
        let a: byte[] = 4;
    }

expect_compile_failure: true
expect_stderr: |
  2:9 Type error: Array length must be specified if shorthand initialization syntax is used
      let a: byte[] = 4;
          ^
  Terminating compilation due to 1 error