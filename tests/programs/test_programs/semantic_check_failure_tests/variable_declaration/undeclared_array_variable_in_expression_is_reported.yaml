program: |
    fn main() : int {
        let a: int = c[4] * 8;
    }

expect_compile_failure: true
expect_stderr: |
  2:18 Name error: Undeclared identifier 'c'
      let a: int = c[4] * 8;
                   ^
  Terminating compilation due to 1 error