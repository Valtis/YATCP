program: |
    fn main() : int {
      let a: bool = !3;
      return 4;
    }

expect_compile_failure: true
expect_stderr: |
  2:18 Type error: Operand must be 'Boolean', got 'Integer' instead
    let a: bool = !3;
                   ^
  Terminating compilation due to 1 error