program: |
    fn main() : int {
      let b: int = 0;
      let a: int = b.length;
      return 0;
    }
    
    
expect_compile_failure: true
expect_stderr: |
  3:18 Type error: Invalid property 'length' for expression of type 'Integer'
    let a: int = b.length;
                   ^^^^^^
  Terminating compilation due to 1 error