program: |
    fn main() : int {
      let val: bool = -"hello";
      return 0;
    }
    
    fn baz(a: int) {

    }
    
expect_compile_failure: true
expect_stderr: |
  2:19 Type error: Cannot negate operand of type 'String'
    let val: bool = -"hello";
                    ^
  Terminating compilation due to 1 error