program: |
    fn main() : int {
      let a: bool = 4 || 2;
      return 4;
    }

expect_compile_failure: true
expect_stderr: |
  2:19 Type error: Operands of type 'Integer' are not valid for this operator
    let a: bool = 4 || 2;
                    ^^
  Terminating compilation due to 1 error