program: |

  fn main() : int {
    let a =  ~3f;
    return 0;
  }

expect_compile_failure: true
expect_stderr: |
  3:13 Type error: Operand must be an integral value, got 'Float' instead
    let a =  ~3f;
              ^^
  Terminating compilation due to 1 error