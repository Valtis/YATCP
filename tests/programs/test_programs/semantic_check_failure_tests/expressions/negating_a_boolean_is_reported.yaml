program: |
    fn main() : int {
      let val: bool = -true;
      return 0;
    }
    
    fn baz(a: int) {

    }
    
expect_compile_failure: true
expect_stderr: |
  2:19 Type error: Cannot negate operand of type 'Boolean'
    let val: bool = -true;
                    ^
  Terminating compilation due to 1 error