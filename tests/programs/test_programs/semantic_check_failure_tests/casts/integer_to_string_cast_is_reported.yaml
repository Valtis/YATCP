program: |

    fn main() : int {
      let x: string = 4 as string;

      return 0;
    }

expect_compile_failure: true
expect_stderr: |
  3:21 Type error: Cannot cast type 'Integer' into type 'String'
    let x: string = 4 as string;
                      ^^
  Terminating compilation due to 1 error