program: |

    fn main() : int {
      if true {

      } else if 4 {
        
      }

      return 0;
    }
    
expect_compile_failure: true
expect_stderr: |
  5:13 Type error: Expected 'Boolean' for if expression but was 'Integer'
    } else if 4 {
              ^
  Terminating compilation due to 1 error