program: |

    fn main() : int {
      if 4 {

      }

      return 0;
    }
    
expect_compile_failure: true
expect_stderr: |
  3:6 Type error: Expected 'Boolean' for if expression but was 'Integer'
    if 4 {
       ^
  Terminating compilation due to 1 error