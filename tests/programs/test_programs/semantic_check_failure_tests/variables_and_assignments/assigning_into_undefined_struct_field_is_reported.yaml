program: |

    struct Foo {
      bar: int;
    }

    fn main() : int {
      let x = Foo {
        bar = 4;
      };
      x.a = 4;
    }
    
  
expect_compile_failure: true
expect_stderr: |
  10:5 Name error: Undefined field 'a' for struct 'Foo'
    x.a = 4;
      ^
  Terminating compilation due to 1 error