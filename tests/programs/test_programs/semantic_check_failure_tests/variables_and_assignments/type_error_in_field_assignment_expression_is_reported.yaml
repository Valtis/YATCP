program: |

    struct Foo {
      a: int;
    }

    fn main() : int {
      let x = Foo {
        a = 4;
      };
      x.a = 44 + "hello, world";
    }
    
  
expect_compile_failure: true
expect_stderr: |
  10:12 Type error: Incompatible operand types 'Integer' and 'String' for this operation
    x.a = 44 + "hello, world";
             ^
  Terminating compilation due to 1 error