program: |

    struct Foo {
      a: int;
    }

    fn main() : int {
      let x = Foo {
        a = 4;
      };
      let byte_val = 4b;
      x.a = byte_val;
    }
    
  
expect_compile_failure: true
expect_stderr: |
  11:7 Type error: Incompatible operand types 'Integer' and 'Byte' for this operation
    x.a = byte_val;
        ^
  11:7 Note: Explicit cast is required to convert 'Byte' to 'Integer'
    x.a = byte_val;
        ^
  Terminating compilation due to 1 error