program: |

    struct Foo {
      a: int;
    }

    fn main() : int {
      let x = Foo {
        a = 4;
      };
      x.a = "hello";
    }
    
  
expect_compile_failure: true
expect_stderr: |
  10:7 Type error: Incompatible operand types 'Integer' and 'String' for this operation
    x.a = "hello";
        ^
  Terminating compilation due to 1 error