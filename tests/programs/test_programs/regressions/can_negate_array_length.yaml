program: |

    extern fn int_printer(value: int);

    fn main() : int {
      let x = { 1, 2, 3, 4, 5};

      // parsing presedence bug: Was parsed as (-x).length
      let y = -x.length;
      int_printer(y);    
      foo(x);

      return 0;
    }

    fn foo(x: int[]) {
      int_printer(-x.length);
    }
link_with:
  - tests/files/support/support.c 
expect_stdout: |
  -5
  -5
