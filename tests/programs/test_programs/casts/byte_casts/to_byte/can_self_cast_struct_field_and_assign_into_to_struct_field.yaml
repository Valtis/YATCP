program: |
    extern fn byte_printer(value: byte);

    struct Foo {
        a: byte;
    }

    fn main() : int {
        let y = Foo {
            a = 100;
        };

        let z = Foo {
            a = y.a as byte;
        };

        byte_printer(z.a);
        z.a = y.a as byte;
        byte_printer(z.a);
        return 0;
    }
link_with:
    - tests/files/support/support.c
expect_stdout: |
  100
  100
  