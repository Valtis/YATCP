program: |
    extern fn short_printer(value: short);

    struct Foo {
        a: byte;
    }

    fn main() : int {
        let y = Foo {
            a = 100;
        };

        let z: short[3] = 5;
        z[0] = y.a as short;
        short_printer(z[0]);
        return 0;
    }
link_with:
    - tests/files/support/support.c
expect_stdout: |
  100

  