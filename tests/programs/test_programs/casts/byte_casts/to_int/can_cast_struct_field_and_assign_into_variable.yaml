program: |
    extern fn int_printer(value: int);

    struct Foo {
        a: byte;
    }

    fn main() : int {
        let y = Foo {
            a = 100;
        };

        let z: int = 5;
        z = y.a as int;
        int_printer(z);
        return 0;
    }
link_with:
    - tests/files/support/support.c
expect_stdout: |
  100

  