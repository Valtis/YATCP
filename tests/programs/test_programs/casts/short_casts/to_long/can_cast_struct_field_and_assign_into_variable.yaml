program: |
    extern fn long_printer(value: long);

    struct Foo {
        a: short;
    }

    fn main() : int {
        let y = Foo {
            a = 100;
        };

        let z: long = 5;
        z = y.a as long;
        long_printer(z);
        return 0;
    }
link_with:
    - tests/files/support/support.c
expect_stdout: |
  100

  