program: |
    extern fn long_printer(value: long);

    struct Foo {
        a: short;
    }

    fn main() : int {
        let y = Foo {
            a = 100;
        };

        let z: long[3] = 5;
        z[0] = y.a as long;
        long_printer(z[0]);
        return 0;
    }
link_with:
    - tests/files/support/support.c
expect_stdout: |
  100

  