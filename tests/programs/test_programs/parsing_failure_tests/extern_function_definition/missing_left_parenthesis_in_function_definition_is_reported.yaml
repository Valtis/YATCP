program: |
    extern fn main);

    fn dummy() { }
expect_compile_failure: true
expect_stderr: |
  1:15 Syntax error: Unexpected token ')' when '(' was expected
  extern fn main);
                ^
  Terminating compilation due to 1 error
