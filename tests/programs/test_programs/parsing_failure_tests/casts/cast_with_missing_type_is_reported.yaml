program: |
    fn main() : int {
        let x: int = 4;
        let b: byte = x as;
    }

expect_compile_failure: true
expect_stderr: |
  3:23 Syntax error: Unexpected token ';' when 'type' was expected
      let b: byte = x as;
                        ^
  Terminating compilation due to 1 error
