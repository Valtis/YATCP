program: |
    fn main() : int {
        let x: int = 4;
        let b: byte = x as fn;
    }

expect_compile_failure: true
expect_stderr: |
  3:24 Syntax error: Unexpected token 'fn' when one of 'identifier', 'double', 'float', 'string', 'void', 'bool', 'long', 'int', 'short', 'byte' were expected
      let b: byte = x as fn;
                         ^^
  Terminating compilation due to 1 error